# Artifacts

Generated files are written here by:

```bash
python scripts/run_demo.py
python scripts/prepare_public_validation.py
```

This directory is ignored by Git because the HTML dashboard, JSON payloads, protocol templates, and reports are reproducible from source.
