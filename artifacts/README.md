# Artifacts

I write generated demo outputs here.

```bash
python scripts/run_demo.py
python scripts/prepare_public_validation.py
```

The HTML dashboard, JSON payloads, runbooks, traces, and PDFs are reproducible, so I ignore them by default.
