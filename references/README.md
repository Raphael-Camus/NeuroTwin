# References

This directory tracks bibliography notes and research context. Local paper PDFs belong in `references/papers/`, which is ignored by Git to avoid committing copyrighted material.

Curated notes are stored under `docs/research/` and `docs/evidence/`.
