# References

I use this directory for local reference management.

Paper PDFs belong in `references/papers/`, which is ignored by Git. When a reference materially changes the project design, I summarize the design implication in `docs/research/research-notes.md` or the relevant architecture document instead of committing the paper itself.
